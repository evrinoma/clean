{#если нужно нестандратно вывести список, то переопредяем шаблон, для этого нужно в файлике knp_menu.yaml установить templating: false#}
{% extends 'knp_menu.html.twig' %}

{#{% block list %}#}
    {#{% import 'knp_menu.html.twig' as macros %}#}
    {#{% if item.hasChildren and options.depth is not same as(0) and item.displayChildren %}#}
        {#<ol{{ macros.attributes(listAttributes) }}>#}
            {#{{ block('children') }}#}
        {#</ol>#}
    {#{% endif %}#}
{#{% endblock %}#}

{#{% block item %}#}
    {#<li></li>#}
{#{% endblock %}#}